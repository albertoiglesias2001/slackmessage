{
  "name": "restcall",
  "version": "1.2.0",
  "description": "restcall",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -p .",
    "postbuild": "npm run package",
    "package": "tfx extension create -r ..",
    "gallery-publish": "tfx extension publish --rev-version"
  },
  "keywords": [
    "rest"
  ],
  "author": "Sylvain Marangon",
  "license": "ISC",
  "dependencies": {
    "request": "^2.81.0",
    "vsts-task-lib": "^2.0.2-preview"
  },
  "devDependencies": {
    "@types/node": "^7.0.12",
    "typescript": "^2.3.2"
  }
}
